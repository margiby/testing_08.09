import type { EdgeTypes } from '@xyflow/react';
import FloatingEdge from './FloatingEdge';

// Edge types für React Flow
const edgeTypes: EdgeTypes = {
  floating: FloatingEdge,
};

export default edgeTypes;